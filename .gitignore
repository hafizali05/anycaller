.aws-sam/
node_modules/
.next/
.env
.env.local
*.log

# Claude Code per-machine settings — project config sits in
# .claude/settings.json; .local.json is the developer's machine.
.claude/settings.local.json
